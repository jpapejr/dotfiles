# dotfiles
My dotfile collection

> reconnect.sh is a simple script that "reconnects" the dotfiles again by symlinking the actual files in this repo to the $HOME directory
where the respective programs will read them (I.e. zsh, vim, etc.)
