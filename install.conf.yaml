- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
      force: true
      path: .zshrc
    ~/.config/starship.toml:
      force: true
      path: starship.toml
    ~/.tmux.conf:
      force: true
      path: .tmux.conf
